server:
  port: 8087

management:
  endpoint:
    health:
      show-details: "always"
  endpoints:
    web:
      base-path: /
      exposure:
        include: health, info, prometheus

config:
  privilegedAccessClients: ${TASK_MANAGEMENT_PRIVILEGED_CLIENTS:wa_task_management_api,xui_webapp,ccd_case_disposer}
  exclusiveAccessClients: ${TASK_MANAGEMENT_EXCLUSIVE_CLIENTS:wa_task_management_api,wa_task_monitor,wa_case_event_handler,wa_workflow_api}
  allowedJurisdictions: ${ALLOWED_JURISDICTIONS:ia,wa,sscs,civil,publiclaw,privatelaw,employment,st_cic}
  allowedCaseTypes: ${ALLOWED_CASE_TYPES:asylum,wacasetype,sscs,civil,generalapplication,care_supervision_epo,prlapps,et_englandwales,et_englandwales_listings,et_englandwales_multiple,et_scotland,et_scotland_listings,et_scotland_multiple,et_admin,privatelaw_exceptionrecord,benefit,CriminalInjuriesCompensation}
  taskMandatoryFieldCheckEnabled: ${MANDATORY_TASK_FIELD_CHECK_ENABLED:true}
  taskMandatoryFields: ${MANDATORY_TASK_FIELDS:taskName,taskId,taskType,dueDateTime,state,securityClassification,title,majorPriority,minorPriority,executionTypeCode,caseId,caseTypeId,caseCategory,caseName,jurisdiction,region,location,created,roleCategory,workTypeResource}
  initiationRequestRequiredFields: ${INITIATION_REQUEST_REQUIRED_FIELDS:name,taskType,caseId}
  search:
    defaultMaxResults: 50
  fieldsToExcludeFromTrim: ${FIELDS_TO_EXCLUDE_FROM_TRIM:description,title,caseName}

core_case_data:
  api:
    url: http://ccd-data-store-api-perftest.service.core-compute-perftest.internal

camunda:
  url: http://camunda-api-perftest.service.core-compute-perftest.internal/engine-rest

role-assignment-service:
  url: http://am-role-assignment-service-perftest.service.core-compute-perftest.internal
  maxResults: ${ROLE_ASSIGNMENT_MAX_RESULTS:50}

security:
  anonymousPaths:
    - "/"
    - "/health"
    - "/health/liveness"
    - "/health/readiness"
    - "/loggers/**"
    - "/swagger-resources/**"
    - "/swagger-ui/**"
    - "/swagger-ui/index.html"
    - "/v3/**"
    - "/favicon.ico"

auth:
  provider:
    service:
      client:
        baseUrl: http://rpe-service-auth-provider-perftest.service.core-compute-perftest.internal

idam:
  baseUrl: https://idam-api.perftest.platform.hmcts.net
  redirectUrl: https://ia-case-api-perftest.service.core-compute-perftest.internal/oauth2/callback
  scope: "openid profile roles"
  s2s-auth:
    url: http://rpe-service-auth-provider-perftest.service.core-compute-perftest.internal
    secret: CB5IYB56IR6I2D4Z
    name: wa_task_management_api
  s2s-authorised:
    services: ${WA_S2S_AUTHORIZED_SERVICES:ccd,ccd_data,ccd_gw,ccd_ps,iac,wa_task_management_api,xui_webapp,wa_task_monitor,camunda_bpm,wa_workflow_api,wa_case_event_handler,ccd_case_disposer}
  api:
    baseUrl: https://idam-api.perftest.platform.hmcts.net
  system:
    username: wa-system-user@hmcts.net
    password: System01

springdoc:
  swagger-ui:
    enabled: true
    operationsSorter: method
  writer-with-order-by-keys: true

replication:
  username: pgadmin
  password: pICLE1zxVYs)SHrDLZcf
  subscriptionUrl: postgresql://cft-task-postgres-db-flexible-replica-perftest.postgres.database.azure.com:5432
primary:
  username: pgadmin
  password: mzf1YNagqGizE)epRJim

spring:
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  config:
    import: "optional:configtree:/mnt/secrets/wa/"
  application:
    name: WA Task Management
  main:
    allow-bean-definition-overriding: true
  datasource:
    driverClassName: org.postgresql.Driver
    jdbcUrl: jdbc:postgresql://cft-task-postgres-db-flexible-perftest.postgres.database.azure.com:5432/cft_task_db?sslmode=require
    username: pgadmin
    password: mzf1YNagqGizE)epRJim
  datasource-replica:
    driverClassName: org.postgresql.Driver
    jdbcUrl: jdbc:postgresql://cft-task-postgres-db-flexible-replica-perftest.postgres.database.azure.com:5432/cft_task_db?sslmode=require
    username: pgadmin
    password: pICLE1zxVYs)SHrDLZcf
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      naming:
        physical-strategy: io.hypersistence.utils.hibernate.naming.CamelCaseToSnakeCaseNamingStrategy
    properties:
      hibernate:
        query:
          in_clause_parameter_padding: true
        default_schema: cft_task_db
        dialect: "uk.gov.hmcts.reform.wataskmanagementapi.config.CustomPostgreSQL94Dialect"
        physical_naming_strategy: "org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy"
  flyway:
    enabled: true
    baselineOnMigrate: true
    defaultSchema: cft_task_db
    schemas: cft_task_db
    placeholders:
      dbPrimaryUserName: pgadmin
      dbReplicaUserName: pgadmin
      dbReaderUserName: pgadmin
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://idam-web-public.perftest.platform.hmcts.net/o/jwks
      client:
        provider:
          oidc:
            issuer-uri: https://idam-web-public.perftest.platform.hmcts.net/o
        registration:
          oidc:
            client-id: wa
            client-secret: 3CN3KBUUEKCKRMKH

feign:
  client:
    config:
      default:
        connectTimeout: 30000
        readTimeout: 30000
        loggerLevel: FULL

launchDarkly:
  sdkKey: sdk-b4ab9713-10d0-43ac-90ec-662e207092c8
  connectionTimeout: 5000
  socketTimeout: 5000

environment: ${ENVIRONMENT:local}

logging:
  level:
    root: INFO
    uk.gov.hmcts.reform.wataskmanagementapi.clients.*: INFO
    org.zalando.logbook: INFO
    org.testcontainers: INFO
    com.github.dockerjava: INFO

logbook:
  include: /task/**

caffeine:
  timeout:
    duration: 30
    unit: MINUTES
  task-type:
    timeout:
      duration: ${TASK_TYPE_CACHE_TIMEOUT_DURATION:60}
      unit: ${TASK_TYPE_CACHE_TIMEOUT_TIMEUNIT:MINUTES}
  calendar:
    timeout:
      duration: ${CALENDAR_CACHE_TIMEOUT_DURATION:24}
      unit: ${CALENDAR_CACHE_TIMEOUT_TIMEUNIT:HOURS}

bankHolidays:
  api:
    url: "https://www.gov.uk"
