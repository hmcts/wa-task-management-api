targets:
  instance: ${TEST_URL:http://localhost:8087}
  camunda: ${CAMUNDA_URL:http://camunda-bpm/engine-rest}
  documentStoreUrl: ${DOCUMENT_STORE_URL:http://dm-store-aat.service.core-compute-aat.internal}
core_case_data:
  api:
    url: ${CCD_URL:http://ccd-data-store-api}

logging.level.uk.gov.hmcts.reform: debug

feign:
  client:
    config:
      default:
        connectTimeout: 30000
        readTimeout: 30000
        loggerLevel: full

